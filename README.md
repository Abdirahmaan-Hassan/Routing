<!-- Axios is a popular Promise-based HTTP client for the browser and Node.js.
It simplifies making HTTP requests and works both in browsers and Node.js environments,
providing features such as handling request and response interception, transformation,
and automatic JSON data parsing.

First, make sure you have Axios,bodyParser installed in your Node.js project.
If you haven't installed it yet, use:

npm install axios
npm install bodyParser 
-->